# Allow install scripts for trusted native dependencies
# This is required for better-sqlite3 to build native bindings
#
# Note: pnpm v10+ requires explicit approval for build scripts
scripts-allow-list[]=better-sqlite3
